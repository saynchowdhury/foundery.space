## 2025-05-15 - Redundant logo alt text
**Learning:** When brand text immediately follows a logo in the DOM (e.g., in a Header), setting the logo's `alt` attribute to an empty string (`alt=""`) prevents screen readers from announcing the company name twice, improving the navigation experience.
**Action:** Always check if a logo is accompanied by visible text and use decorative alt attributes where appropriate.
